/*document.getElementById("home-nav-id").addEventListener('click', function (e) {
    //e.preventDefault();
    document.getElementById('nav-form').action = "/"
});



function homeRedirectFunction(e) {
    //e.preventDefault(); // Prevents the annoying bug where it doesn't redirect when pressing the nav item
    document.getElementById('nav-form').action = "/"
}
    */